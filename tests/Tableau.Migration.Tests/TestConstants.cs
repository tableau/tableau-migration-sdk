using System;

namespace Tableau.Migration.Tests
{
    public static class TestConstants
    {
        public const string LocalhostUrl = "https://localhost";
        public static readonly Uri LocalhostUri = new(LocalhostUrl);
    }
}
